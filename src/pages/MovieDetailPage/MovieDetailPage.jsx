import { useState, useRef, useEffect } from 'react';
import sendRequest from '../../utilities/send-request';

export default function MovieDetailPage (props){
    const [movie, setMovie] = useState(null)
    return (
        <div><h1> Movie Detail Page</h1>

        </div>
    )
}